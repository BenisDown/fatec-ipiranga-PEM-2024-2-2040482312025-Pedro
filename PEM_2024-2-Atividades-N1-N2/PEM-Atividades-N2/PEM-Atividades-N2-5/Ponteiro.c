#include <stdio.h>
#include <string.h>

struct Produto{
    int id;
    char nome[50];
    int qtde;
    float valor;
};

void cadastrar{
    printf("Digite o ID do novo produto: %d", id);
    scanf("%d", id);
    printf("Digite o nome do produto: %s", nome);
    scanf("%s", str);
    printf("Digite a quantidade do produto: %d", qtde);
    scanf("%d", qtde);
    printf("Digite o valor do produto: %f", valor);
    scanf("%f", valor);
};
